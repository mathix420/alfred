# PlatformIO build workspace
.pio/

# ESP-IDF / CMake build output
build/

# Generated kconfig — seeded from sdkconfig.defaults (committed). Regenerate by
# deleting these and rebuilding.
sdkconfig
sdkconfig.old
sdkconfig.companion
sdkconfig.companion_v1
sdkconfig.*.old

# Firmware images
*.bin

# Per-developer / managed component lock (regenerated by the component manager)
managed_components/
dependencies.lock
